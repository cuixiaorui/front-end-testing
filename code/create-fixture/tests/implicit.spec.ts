import { it, expect, describe, beforeEach } from "vitest";
import { addTodo } from "../index";

describe("隐式", () => {
	let todoA
	let todoB
	let todoC
	beforeEach(() => {
		todoA
		todoB
		todoC
		todoD
		
	})


	it('(should )', () => {
		
	});

});
